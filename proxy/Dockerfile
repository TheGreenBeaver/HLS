FROM nginx

ARG PROXY_KIND
ARG EXTERNAL_HTTP_PORT
ARG EXTERNAL_HTTPS_PORT

COPY templates/${PROXY_KIND}.conf.template /etc/nginx/templates/default.conf.template

EXPOSE ${EXTERNAL_HTTP_PORT}
EXPOSE ${EXTERNAL_HTTPS_PORT}